import CaseModel from "./cases.model.js";
import PartnerConfigModel from "./partnerConfig.model.js";

const models = {
  Case: CaseModel,
  PartnerConfig: PartnerConfigModel,
};

export default models;
